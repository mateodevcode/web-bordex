/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["i.postimg.cc", "dummyimage.com", "i.blogs.es", "drive.google.com", "media.giphy.com", "visionyvalor.es"],
    },
};

export default nextConfig;
